{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "FileAnalysisService": "Debug"
    }
  },
  "ConnectionStrings": {
    "PostgresConnection": "Host=localhost;Port=5432;Database=textscanner_fileanalysis_dev;Username=postgres;Password=postgres_password;Include Error Detail=true"
  },
  "ServiceEndpoints": {
    "FileStoringService": {
      // Для локальной разработки, когда FileStoringService запущен на этом порту:
      "BaseUrl": "http://localhost:5001/" // Заканчивается на / для удобства конкатенации с относительными путями API
    }
  }
}